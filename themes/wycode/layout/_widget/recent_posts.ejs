<% if (site.posts.length){ %>
  <div class="widget-wrap">
    <h4 class="widget-title"><i class="far fa-clock"></i> <%= __('recent_posts') %></h4>
    <div class="widget">
      <ul class="recent-list">
        <% site.posts.sort('date', -1).limit(5).each(function(post){ %>
          <li class="recent-list-item">
            <a href="<%- url_for(post.path) %>"><%= post.title || '(no title)' %></a>
          </li>
        <% }); %>
      </ul>
      <a class="all-post-link" href="<%- url_for(theme.menu.archives) %>"><i class="fa fa-folder-open"
                                                                                aria-hidden="true"></i> 查看全部</a>
    </div>
  </div>
<% } %>